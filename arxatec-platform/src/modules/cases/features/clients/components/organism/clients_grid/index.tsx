import { CardClient } from "../../molecules";

export const ClientsGrid = () => {
  return (
    <div className="grid grid-cols-3 gap-2">
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
      <CardClient />
    </div>
  );
};
