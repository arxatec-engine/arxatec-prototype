---
export const prerender = true;

export function get() {
  return new Response(null, {
    status: 302,
    headers: {
      Location: '/es', // Redirige a /es al cargar la página principal
    },
  });
}
---
